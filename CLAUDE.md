# GEMOVIE — პროექტის გზამკვლევი (Claude, წაიკითხე ეს ჯერ)

ქართული ფილმების/სერიალების საიტი, **მთლიანად ge.movie-ს მიხედვით** (დიზაინი, კატეგორიები,
ფლეერი, მონაცემი). TMDB **აღარ გამოიყენება** — ყველაფერი ge.movie-დანაა.

- **სამუშაო საქაღალdე:** `C:\Users\Asus\Desktop\gemovie-build` (ყველა ფაილი აქ არის — Downloads აღარ სჭირდება)
- **სტეკი:** Python + Flask + SQLAlchemy + SQLite (`gemovie.db`). Frontend — ge.movie-ს CSS/JS თემა + AJAX.
- **ვირტ. გარემო:** `.venv` (Windows). Python: `.venv/Scripts/python.exe`

## გაშვება
```bash
.venv/Scripts/python.exe app.py      # → http://127.0.0.1:5000
```
launch.json-ში კონფიგი ჰქვია **"GEMOVIE (Flask)"** (fluentra-build-ის .claude/launch.json-შია).
ტესტისას ქართული ტექსტისთვის: `PYTHONIOENCODING=utf-8`.

## რა არის უკვე გაკეთებული
- ge.movie-ს სრული კატალოგი დაskреიპილი და ბაზაშია: **~10 100 ფილმი + ~1 275 სერიალი**,
  ~11 400 ფლეერი, ~24 ჟანრი, **1 000 მსახიობი**, **11 388 თრეილერი**.
- ge.movie-ს **დიზაინი ატანილია** (გვერდითი მენიუ, header, ფერები/ფონტები — FiraGO).
- **მთავარი გვერდი:** ჰერო = მარცხნივ დიდი ფილმი + მარჯვნივ 3×3 ესკიზების ბადე (ge.movie-ვით),
  ქვემოთ AJAX რიგები (ტრენდში/ახლახან/სერიალები/მაღალი რეიტინგი) აიქონებით.
- **ფილტრი/დათვალიერება** (`/browse`, `/filter-movies`): 5-სვეტიანი landscape ბადე,
  ფილტრები (კატეგორია/ჟანრი/წელი/დალაგება) + **infinite scroll**, ყველაფერი AJAX-ით.
- **დეტალური გვერდი** (`/movie/<id>`, `/series/<id>`): ge.movie-ს დიზაინი — მარცხნივ პოსტერი +
  თრეილერის ღილაკი, მარჯვნივ ფლეერი; ქვემოთ სათაური + ORIGINAL (caps) + სამოქმ. ღილაკები
  (♥/მოგვ./შეტყ./დახმარება), IMDb + ვარსკვლავები, ინფო-სია (ჟანრი/სტუდია/წელი/რეჟისორი/ხანგრძ./
  ქვეყანა — ცარიელი ველი იმალება), „ფილმის სიუჟეტი", „მსახიობები" (cast-ის არსებობისას) + „მსგავსი".
  **NB:** studio/director/country/runtime/cast მხოლოდ დეტალურ გვერდზეა ge.movie-ზე → საჭიროა
  `01_movies.js`-ის ხელახალი გაშვება, რომ ეს ველები ჩამოიტვირთოს (ld+json-იდან).
- **მსახიობები** (`/persons`): მრგვალფოტოებიანი ბადე, infinite scroll (ფოტოიანები წინ).
- **თრეილერები** (`/browse?type=trailer`): ფილმები თრეილერით; დეტალურზე თრეილერი YouTube-ით უკრავს.

## ბექოფისი (2026-09-11, ეტაპები 1 და M3)
`/admin` აღარ არის ძველი „Sync Movies" გვერდი (მოხსნილია TMDB-თან ერთად). ახლა იქ არის
სესიით დაცული ბექოფისი: **დაფა**, **კონტენტი** (ფილმის/სერიალის სია + თითოეულის სურათები),
**ბრენდინგი** (ლოგო/favicon/პოპაპ-ბანერი: ატვირთვა და წაშლა) და **მედია-ბიბლიოთეკა**.

ფილმის როლები: `backdrop` (ბარათი და დეტალურის ფონი), `poster` (ვერტიკალური),
`hero` (მთავარის დიდი ბლოკი). ბექოფისის სურათი ყოველთვის ჯობნის `poster_url`-ს, იმპორტი
მას ვერ შლის (ცალკე ცხრილშია). მოხსნისას საიტი ორიგინალს უბრუნდება.
`_has_poster()` ახლა CMS-სურათსაც ითვლის, ანუ გატეხილი `poster_url`-ის მქონე 21 ჩანაწერი
ბექოფისიდან სურათის დაყენებისთანავე ჩნდება საიტზე.

- გეგმები: `docs/BACKOFFICE-PLAN.md` (მთელი ბექოფისი) და `docs/MEDIA-CMS-PLAN.md` (სურათები).
- ადმინის შექმნა: `.venv/Scripts/python.exe -m flask create-admin <user> --password <pass>`
  (წინ `FLASK_APP=app`). არსებულ მომხმარებელს პაროლს უცვლის.
- არსებული ლოგო/favicon/ბანერი ბაზაში: `... -m flask seed-branding` (იდემპოტენტურია).
- ტესტი: `.venv/Scripts/python.exe tests/test_backoffice.py` (~50 შემოწმება: დაცვა, CSRF, ლოგო,
  ფილმის სურათები, მთავარი გვერდის რიგები. თავის შემდეგ ალაგებს და პარამეტრებს აღადგენს).

**მთავარი გვერდის კონსტრუქტორი** (`/admin/home`): 11 რიგი აღარ არის `index.html`-ში ჩაწერილი,
`home_row` ცხრილშია. თითოზე: სათაური, აიქონი, კონტენტის ტიპი, წყობა, ჟანრი/გამორიცხული ჟანრი,
გარეგნობა (landscape/portrait/top9), რაოდენობა, ჩართვა, სეზონური ფანჯარა (MM-DD).
თანმიმდევრობა ↑/↓ ღილაკებით. `home_rows.py` ინახავს ნაგულისხმევ წყობას; ცხრილის ცარიელობისას
`index.html` ძველ, ჩაწერილ რიგებს ხატავს (საიტი არასდროს რჩება ცარიელი).
ჰეროს დაპინული ფილმები, გამორიცხული ჟანრი და კვირის ტოპის წლები/სიგრძე `Setting`-ებშია.
CLI: `flask seed-home-rows [--replace]`.

**უნივერსალური სურათის სლოტი (2026-09-12):** ერთი წყვილი endpoint-ი ემსახურება ყველაფერს:
`POST /admin/image/<subject_type>/<subject_id>/<role>/{upload|fetch|pick|delete}` (`admin/images.py`).
subject_type: site (subject_id=0), movie, series, person, menu_item, home_row, genre.
UI არის ერთი Jinja მაკრო `templates/admin/_imageslot.html` (**იმპორტი `with context`-ით,
თორემ csrf_token მაკროში არ ჩანს**). ახალი ადგილის დამატება = ახალი როლი `service.ALL_ROLES`-ში.

**რა იმართება ბექოფისიდან:** ლოგო (3 ვარიანტი), favicon, OG, placeholder-ები, პოპაპ-ბანერი,
**საიტის ფონი**, **მენიუს ყველა პუნქტი** (`menu_item` ცხრილი: სახელი, ბმული, აიქონი ან ატვირთული
სურათი, თანმიმდევრობა, ხილვადობა desktop/mobile) — SVG-ები `templates/_menu_icons.html`-შია, ბაზაში
ნედლი markup არ ინახება; **მთავარი გვერდის რიგები** + თითოეულის **ფონი**; ფილმის backdrop/poster/hero;
**მსახიობის ფოტო** (`/admin/persons`, `/api/persons` CMS-ფოტოს ამჯობინებს).
CLI: `flask seed-menu [--replace]`.

**გადაწყვეტილება (2026-09-12): სურათები გარე წყაროდან რჩება.** მასობრივი mirroring გაჩერდა
4 613 ჩანაწერზე და ყველა მიბმა მოიხსნა — საიტი ისევ ge.movie/TMDB-დან იღებს ფილმების სურათებს.
CMS მხოლოდ იმას მართავს, რაც ჩვენია: ლოგო, favicon, ბანერები, ფონები, მენიუს აიქონები.
ცალკეული ფილმის სურათის ხელით ატვირთვა/ჩამოტანა კვლავ შესაძლებელია, უბრალოდ მასობრივად აღარ ხდება.

**ზომები ბექოფისშია ნაჩვენები.** თითო როლს აქვს `size` და `size_note` (`service.ALL_ROLES`),
გაზომილი ნამდვილ საიტზე 1920px ეკრანზე: რიგის ფონი 1920×480 (ჩანს 1910×420), პოპაპი 1400×200
(ჩანს 700×100), ჰერო 1920×960 (ჩანს 1128×560), ბარათი 1280×720 (ჩანს 367×206), ვერტიკალური
600×900 (ჩანს 152×228), აიქონი 64×64, favicon 512×512. სლოტი გვერდიგვერდ აჩვენებს
დაყენებულს და **ახლანდელს** (რა დგას იმ ადგილას ამჟამად).

**რიგის შიგთავსი (`home_row_item` + `admin/home_items.py`):** თითო რიგს შეიძლება ჰქონდეს
ფიქსირებული ფილმების სია. თუ აქვს, საიტი ზუსტად მას აჩვენებს, ამ თანმიმდევრობით;
თუ არა, ძველებურად ავტომატურად ივსება. სამი გზა: **შაფლი** (შემთხვევითი შერჩევა იმავე
კატეგორიიდან, `row_candidates()` იგივე ფილტრებით), **ხელით ძებნა-დამატება**
(`/admin/home/search`), **გადათრევა**. გასუფთავება რიგს ავტომატურ რეჟიმში აბრუნებს.
გადაცემა: `index.html` → `data-ids="movie:1,tv:2"` → `app.js` → `/api/movies?ids=...`
(ახალი ветка `api_movies`-ში, ზუსტ რიგითობას ინახავს). გადათრევა ორივე დონეზეა
(რიგები და ფილმები), `static/admin/home-builder.js`, ბიბლიოთეკის გარეშე.
თითო ფილმზე ასევე: **ჩანაცვლება ადგილზე** (`/items/<pk>/replace` — ნომერი რჩება, დუბლიკატი ვერ ჩაჯდება)
და **ნომრის პირდაპირ აკრეფა** (`/items/<pk>/move-to` — დიაპაზონს გარეთ მნიშვნელობა იჭრება).

**ფონური ამოცანები** (`/admin/jobs`): `Job` ცხრილი + `medialib/jobs.py` (thread + პროგრესი
ბაზაში + გაჩერება). `medialib/mirror.py` გარე სურათებს ჩვენს საცავში იღებს და `backdrop`
როლზე აბამს; ვერტიკალურ სურათს (ანიმეს ჰოსტები) `poster` როლზეც. **TMDB-ს უპირატესობა:**
moviege-ს პოსტერების 92.7%-ს ფაილის სახელად TMDB-ის 27-სიმბოლოიანი ჰეში აქვს, ამიტომ
`image.tmdb.org/t/p/original/<hash>.jpg`-დან იღება (Cloudflare-ის გარეშე, უკეთესი ხარისხით).
იდემპოტენტურია: უკვე მიბმულს გამოტოვებს, ანუ ხელახლა გაშვება იქიდან აგრძელებს სადაც შეწყდა.
⚠️ SQLite ახლა **WAL** რეჟიმშია (`tune_sqlite`), თორემ ფონური ჩაწერა საიტს დაბლოკავდა.
გიტში ბაზის commit-მდე: `PRAGMA wal_checkpoint(TRUNCATE); PRAGMA journal_mode=DELETE;`

⚠️ **ხარვეზის ტიპი, რომელიც ორჯერ გამოჩნდა: „სლოტი არის, მაგრამ არაფერს აკეთებს".**
ატვირთვა ბექოფისში მუშაობს, საიტი კი ატვირთულს უგულებელყოფს, რადგან CSS/JS ზემოდან წერს.
დაფიქსირებული და გასწორებული: პრემიერას რიგი (hover-ი გადააწერდა), ტოპ-9 რიგი (ფონს საერთოდ
არ კითხულობდა), მსახიობის ჩამნაცვლებელი (JS-ში არსად იკითხებოდა), მობილურის/მუქი ლოგო
(base.html-ს არ ჰქონდა `<picture>`), ბოლოს დამატებული ეპიზოდები (api_recent_episodes CMS-ს ვერ ხედავდა).
**ახალი როლის დამატებისას აუცილებლად შეამოწმე, საიტი მართლა კითხულობს თუ არა** — ტესტში
შესაბამისი ბლოკია („სლოტი არის, მაგრამ არაფერს აკეთებს" კლასის რეგრესიები).

**მედიის არქიტექტურა:** `medialib/` (storage → images → service → routes).
`MediaAsset` = ფაილი, `MediaVariant` = წარმოებული ზომა, `MediaLink` = (subject, role) → asset.
სურათის ველი არსებულ ცხრილებს **არ ემატება**. საჯარო მისამართი: `/m/<id>/<name>-<sha8>.<ext>`,
სამუდამო ქეშით. შაბლონები სურათს `brand` ობიექტიდან იღებენ (`app.py inject_globals`).

**deploy-ის უსაფრთხოება (გადაწყვეტილი):** `media/` გიტში არ მიდის, `gemovie.db` კი მიდის.
ახალ სერვერზე ფაილები ჯერ არ იქნება, მაგრამ **საიტი არ ტყდება**: `/m/` ვერ პოვნისას
302-ით ამისამართებს `media_asset.source_url`-ზე (ორიგინალ, გარე სურათზე), მოკლე ქეშით.
ფაილების ატვირთვისთანავე თავისით სწორდება. დამხმარე კომანდები:
`flask media-verify [--full]` (რა აკლია) და `flask media-push` (ლოკალური media/ → R2).
R2-ის რეჟიმში boto3 მხოლოდ ჩაწერისთვის იტვირთება, გვერდების გაცემას არ სჭირდება.

**საცავი:** `MEDIA_BACKEND=local` (ნაგულისხმევი, `MEDIA_ROOT`, ლოკალურად `<პროექტი>/media/`,
გიტიდან გარეთ) ან `r2` (S3-თავსებადი, requirements-ში boto3-ის ხაზი უნდა გაიხსნას).
⚠️ **Railway-ზე volume აუცილებელია** (`MEDIA_ROOT=/data/media`), თორემ ატვირთული ფაილი
შემდეგ deploy-ზე ჩუმად ქრება. `check_media_root()` ამაზე ლოგში ხმამაღლა წერს.

## სტრუქტურა
```
app.py            Flask აპი: გვერდები + JSON API (/api/movies, /api/persons, /api/search)
models.py         Movie, Series, Stream, Genre, Person
import_streams.py ფილმების/სერიალების იმპორტი  →  data/movies.json
import_persons.py მსახიობების იმპორტი          →  data/persons.json
import_trailers.py თრეილერების იმპორტი (trailer_key) → data/trailers.json
config.py, sync.py, tmdb.py   (sync/tmdb ЛEGACY — აღარ გამოიყენება; auto-sync გამორთულია app.py-ში)
templates/        base.html (shell), index.html (ჰერო+რიგები), browse.html (ფილტრი),
                  movie.html (დეტალური), persons.html, _moviecard.html (macro)
static/theme/     ge.movie-ს ნამდვილი თემა (CSS/JS/ფონტები) — ლოკალურად სარკირებული
static/css/ge-custom.css   ჩვენი დამატებითი/გადამფარავი CSS (ge.movie-ს იერზე)
static/js/app.js  frontend: ჰერო, რიგები, ფილტრი+infinite scroll, ძებნა, ბარათები
data/             წყარო-მონაცემი (ქვემოთ)
scrapers/         ge.movie-ს ამომღები Console-სკრიპტები + README (ხელახალი scrape-ისთვის)
archive/          ძველი/გამოუყენებელი seed-ები (kinomigma, sample) — არ გამოიყენება
```

## data/ — წყარო-მონაცემი
| ფაილი | შიგთავსი | იმპორტი |
|---|---|---|
| `data/movies.json`   | ge.movie ფილმები+სერიალები (id, title, title_en, type, year, rating, genre, description, poster, player + **director, studio, country, runtime, cast[]**) | `python import_streams.py` |
| `data/persons.json`  | მსახიობები (id, name „ka / en", photo, url) | `python import_persons.py` |
| `data/trailers.json` | თრეილერები (id, type, trailer=videodb URL с YouTube key) | `python import_trailers.py` |
| `data/browser_state.json` | ge.movie-ს სესიის ქუქიები (მხოლოდ ბრაუზერისთვის) | — |

**ბაზის თავიდან აწყობა:** გაუშვი სამივე import სკრიპტი (თანმიმდევრობა: movies → trailers → persons).

## მონაცემის წყარო (მნიშვნელოვანი)
ge.movie **Cloudflare-ის უკანაა** — სერვერიდან პირდაპირ ვერ ვskреიპავთ (403).
მონაცემი ამოღებულია **ბრაუზერის Console-ში** (მომხმარებელი გადის Cloudflare-ს), `scrapers/`-ის
სკრიპტებით. ბაზა უკვე აწყობილია `data/`-დან; ხელახლა მხოლოდ მაშინ, თუ ge.movie განახლდა.
დეტალები: `scrapers/README.md`.

## ცნობილი შეზღუდვები / დასახვეწი
- **პორტრეტული პოსტერი არ გვაქვ** — ge.movie-ს დეტალურ გვერდზე ის JS-ით ისმება და HTML-ში
  არაა; ჩვენს scrape-ში ვერ მოხვდა. ამიტომ ბარათებში/პოსტერში **landscape backdrop** (16:9) გამოიყენება.
- გარე embed-ფლეერები (videodb.stream, em.filmx.my...) შეიძლcan referer-ს ამოწმებდნენ — localhost-ზე
  ზოგი შეიძლება არ დაუკრას (თრეილერი YouTube-ით საიმედოდ უკრავს).
- სერიალებს ცალკე ეპიზოდების არჩევანი არ აქვთ (თითო serial-ს ერთი embed).
- მსახიობის ბარათზე დაჭერით ფილმოგრაფია ჯერ არ იხსნება (`/person/<id>` გვერდი არ არსებობს).
- გვერდითი მენიუ სტაბილურია ≥768px; მობილურ ვიწრო ეკრანზე ge.movie-ს responsive-ს ვცვლით ge-custom.css-ში.
